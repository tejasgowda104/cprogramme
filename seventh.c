#include<stdio.h>
float main()
{
float a,b,c;
printf("key of the first value");
scanf("%f", &a);
printf("key of the second value");
scanf("%f", &b);
c=a-b;
printf("the input value of a and b %.2f%.2f\n",a,b);
printf("the subraction of two numbers is c=%.2f\n",c); 
}
